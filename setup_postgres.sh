#! /bin/bash

service postgresql start
#postgres psql -c "CREATE USER pick_an_otm_username ENCRYPTED PASSWORD 'pick_a_password'"
#postgres psql template1 -c "CREATE EXTENSION IF NOT EXISTS hstore"
#postgres psql template1 -c "CREATE EXTENSION IF NOT EXISTS fuzzystrmatch"
#postgres psql -c "CREATE DATABASE pick_an_otm_db_name OWNER pick_an_otm_username"
#postgres psql pick_an_otm_db_name -c "CREATE EXTENSION IF NOT EXISTS postgis"

